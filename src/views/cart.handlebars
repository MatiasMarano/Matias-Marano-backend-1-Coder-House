<h1>{{title}}</h1>

{{#if cart.products.length}}
  <ul>
  {{#each cart.products}}
    <li>
      <b>{{this.product.title}}</b> — ${{this.product.price}} x {{this.quantity}}
    </li>
  {{/each}}
  </ul>
{{else}}
  <p>Carrito vacío</p>
{{/if}}
